const createUser = ({ name: string, isActive: boolean }) => {

}
let newUser = { name: 'gerry', isActive: true, email: 'g@g.com' }

createUser(newUser)

// // function returning an object

function createCourse(): { name: string, isActive: boolean, email: string } {
    return { name: 'gerry', isActive: true, email: 'g@g.com' }
}
export { }

